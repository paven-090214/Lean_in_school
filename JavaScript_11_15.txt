window.onload -> 모든 html의 코드가 실행 된 이후 window.onload 이벤트가 실행됨
접근 방법
1. class
2. id
3. input -> name

getElementsByTagName
getElementsByClassName
getElementsByTagNameNS
querySelectAll
-> 배열로 출력

p span {} -> 자손
p>span {} -> 자식
    ex). h1>p -> h1의 바로 아래 p만 적용
         h1 p -> h1의 p를 모두 적용

document.querySelector -> 하나만 적용
document.querySelectorAll -> 복수로 적용